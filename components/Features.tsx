
// This component is no longer in use and has been replaced by DashboardSection.tsx.
// It is kept to avoid breaking file system assumptions, but contains no active code.
const Features = () => null;
export default Features;
